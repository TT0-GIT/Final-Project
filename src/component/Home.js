import React from "react";
 
const Home = ({ items }) => {
  
  console.log("look here : " + items[0])
  return (
    <div>
      <h1>Home Page:(slides show)</h1>
    </div>
  );
};

export default Home;
